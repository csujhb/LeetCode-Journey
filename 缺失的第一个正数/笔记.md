

---

# LeetCode 41. 缺失的第一个正�?学习笔记

## 1. 题目描述

给你一个未排序的整数数�?`nums`，请你找出其�?**没有出现的最小正整数**�?

要求�?

* 时间复杂度为 `O(n)`
* 只使用常数级别额外空�?

---

## 2. 示例

### 示例 1

```text
输入：nums = [1,2,0]
输出�?
```

解释：`1` �?`2` 都在数组中，最小缺失正数是 `3`�?

---

### 示例 2

```text
输入：nums = [3,4,-1,1]
输出�?
```

解释：`1` 在数组中，但 `2` 不在数组中�?

---

### 示例 3

```text
输入：nums = [7,8,9,11,12]
输出�?
```

解释：最小正�?`1` 没有出现�?

---

## 3. 题目本质

这题看起来是在“找一个数”，但本质上是在问：

> 对于长度�?`n` 的数组，最小缺失正数可能落在哪个范围？

答案是：

> **只可能在 `1 ~ n+1` 之间**

为什么？

* 如果数组中没�?`1`，答案就�?`1`
* 如果数组中有 `1,2,3,...,n`，那么答案就�?`n+1`
* 所以我们根本不需要关心：

  * 小于等于 `0` 的数
  * 大于 `n` 的数

---

## 4. 核心思路：原地哈�?/ 置换到位

这题的最优解法是�?

> **把值为 `x` 的数尽量放到下标 `x-1` 的位置上**

也就是说�?

* `1` 应该放在 `nums[0]`
* `2` 应该放在 `nums[1]`
* `3` 应该放在 `nums[2]`
* ...
* `n` 应该放在 `nums[n-1]`

这样做完以后，再扫描一遍数组：

* 如果 `nums[0] != 1`，答案就�?`1`
* 如果 `nums[1] != 2`，答案就�?`2`
* ...
* 第一个不匹配的位�?`i`，答案就�?`i + 1`

如果都匹配，说明 `1 ~ n` 都在，答案就�?`n + 1`

---

## 5. 为什么能这样�?

对于一个长度为 `n` 的数组：

* 我们只关�?`1 ~ n`
* 每个合法数字 `x` 都有一个“应该待的位置”：

[
x \rightarrow x - 1
]

所以这题可以把数组本身当成一个“哈希表”来用：

* 数值是 key
* 下标位置就是它应该去的槽�?

这就是所谓的�?

* 原地哈希
* Index Placement
* Cyclic Sort 思想

---

## 6. 标准算法流程

### 第一步：遍历数组，把合法数字放到正确位置

对于当前元素 `nums[i]`�?

如果它满足：

* `nums[i] >= 1`
* `nums[i] <= numsSize`

那么它应该去的位置就是：

```c
nums[i] - 1
```

于是不断交换，直到遇到以下情况之一�?

1. 当前值不�?`1 ~ numsSize` 范围�?
2. 当前值已经在正确位置
3. 目标位置已经有相同值（避免死循环）

---

### 第二步：扫描数组找答�?

遍历数组�?

* 如果 `nums[i] != i + 1`，说�?`i+1` 缺失，直接返�?
* 如果所有位置都满足，返�?`numsSize + 1`

---

## 7. C语言代码实现

```c
int firstMissingPositive(int* nums, int numsSize) {
    for (int i = 0; i < numsSize; i++) {
        while (nums[i] >= 1 &&
               nums[i] <= numsSize &&
               nums[nums[i] - 1] != nums[i]) {
            int target = nums[i] - 1;
            int temp = nums[target];
            nums[target] = nums[i];
            nums[i] = temp;
        }
    }

    for (int i = 0; i < numsSize; i++) {
        if (nums[i] != i + 1) {
            return i + 1;
        }
    }

    return numsSize + 1;
}
```

---

## 8. 执行过程示例

---

### 示例一

```text
nums = [1,2,0]
```

数组长度 `n = 3`，我们只关心 `1~3`

#### i = 0

* `nums[0] = 1`
* 应该在位�?`0`
* 已正确，不动

#### i = 1

* `nums[1] = 2`
* 应该在位�?`1`
* 已正确，不动

#### i = 2

* `nums[2] = 0`
* 不在 `1~3` 范围内，忽略

最终数组：

```text
[1,2,0]
```

扫描�?

* `nums[0] == 1`
* `nums[1] == 2`
* `nums[2] != 3`

所以答案是�?

```text
3
```

---

### 示例�?

```text
nums = [3,4,-1,1]
```

#### i = 0

* `nums[0] = 3`
* 应去位置 `2`
* 交换后：

```text
[-1,4,3,1]
```

* 此时 `nums[0] = -1`，不合法，停�?

#### i = 1

* `nums[1] = 4`
* 应去位置 `3`
* 交换后：

```text
[-1,1,3,4]
```

* 此时 `nums[1] = 1`
* 应去位置 `0`
* 再交换：

```text
[1,-1,3,4]
```

* 此时 `nums[1] = -1`，停�?

#### i = 2

* `nums[2] = 3`
* 已在正确位置

#### i = 3

* `nums[3] = 4`
* 已在正确位置

最终数组：

```text
[1,-1,3,4]
```

扫描�?

* `nums[0] == 1`
* `nums[1] != 2`

所以答案是�?

```text
2
```

---

### 示例�?

```text
nums = [7,8,9,11,12]
```

所有数都大�?`n = 5`，都不在 `1~5` 范围内，全部忽略�?

扫描时：

* `nums[0] != 1`

所以答案直接是�?

```text
1
```

---

## 9. 为什么你的原代码会超�?

你原来的代码是：

```c
while (nums[i] <= numsSize && nums[i] >= 1) {
    if (nums[i] - 1 != i) {
        int temp = nums[nums[i] - 1];
        nums[nums[i] - 1] = nums[i];
        nums[i] = temp;
    } else {
        break;
    }
}
```

问题在于�?

> **没有判断目标位置上是否已经有相同的�?*

这会导致重复元素时发生死循环�?

例如�?

```text
nums = [1,1]
```

�?`i = 1` 时：

* `nums[1] = 1`
* 它想去位�?`0`
* 但位�?`0` 上已经是 `1`
* 交换前后数组完全不变
* 于是无限交换，导致超�?

---

## 10. 关键防死循环条件

必须加上�?

```c
nums[nums[i] - 1] != nums[i]
```

也就是：

> 只有目标位置上的值不是它自己时，才交�?

这就是正确代码中的核心条件：

```c
while (nums[i] >= 1 &&
       nums[i] <= numsSize &&
       nums[nums[i] - 1] != nums[i])
```

---

## 11. 为什么时间复杂度�?`O(n)`

虽然代码里有�?

* 一�?`for`
* 里面还有一�?`while`

看起来像 `O(n^2)`，但实际上不是�?

原因是：

> 每次交换，至少会把一个数字放到它最终应该在的位置上

一个元素不会被无限次“正确归位”，所以总交换次数是线性的�?

因此总时间复杂度为：

```text
O(n)
```

---

## 12. 复杂度分�?

### 时间复杂�?

`O(n)`

### 空间复杂�?

`O(1)`

因为没有使用额外数组或哈希表，只在原数组上进行交�?�?

---

## 13. 易错点总结

### 13.1 不是所有数字都需要处�?

只处理：

```text
1 <= nums[i] <= numsSize
```

因为其他数字不可能影响答案�?

---

### 13.2 交换前必须判断目标位置是否已有相同�?

否则重复元素会死循环�?

错误写法容易类似这样�?

```c
while (nums[i] >= 1 && nums[i] <= numsSize) {
    swap(nums[i], nums[nums[i] - 1]);
}
```

这样在重复元素情况下会卡死�?

---

### 13.3 不能排序

虽然排序后也能找答案，但排序复杂度通常是：

```text
O(n log n)
```

不满足题目要求的 `O(n)`�?

---

### 13.4 不能直接开哈希�?

直接用额外数组或哈希表能做，但空间复杂度不是 `O(1)`�?

---

## 14. 这道题的模板思想

以后看到这类题：

* 数组长度�?`n`
* 数值和下标之间可以建立对应关系
* 要求 `O(n)` 时间
* 要求 `O(1)` 额外空间

优先考虑�?

> **把元素放到它应该去的位置�?*

这种题的典型模板是：

```c
for (int i = 0; i < n; i++) {
    while (当前值合�?&& 不在正确位置 && 目标位不是重复�? {
        交换到目标位�?
    }
}
```

然后再扫描答案�?

---

## 15. 和计数排序思想的联�?

这题虽然不是计数排序，但它和“桶”的思想很像�?

* �?`1` 属于位置 `0`
* �?`2` 属于位置 `1`
* ...
* �?`n` 属于位置 `n-1`

可以理解成一种“只关心合法范围内数字的原地桶化”�?

---

## 16. 面试答题模板

如果面试官问思路，可以这样表述：

> 由于数组长度�?`n`，最小缺失正数只可能�?`1~n+1` 范围内，因此只需要关注这个范围中的数�?
> 对于每个合法数字 `x`，它应该放在下标 `x-1` 的位置上�?
> 所以遍历数组时，如果当前数字在 `1~n` 范围内，且它还没有放到正确位置，同时目标位置不是相同数字，就交换它到对应位置�?
> 这样第一轮处理结束后，如果数组中某个位置 `i` 上不�?`i+1`，说�?`i+1` 缺失，它就是答案�?
> 如果 `1~n` 都在正确位置上，则答案是 `n+1`�?
> 这种方法只在原数组上交换元素，所以时间复杂度�?`O(n)`，额外空间复杂度�?`O(1)`�?

---

## 17. 一句话总结

> **�?`1~n` 范围内的数字原地放到对应下标位置，最后找第一�?`nums[i] != i+1` 的位置�?*

---

## 18. 适合记住的结�?

### 结论 1

最小缺失正数一定在�?

```text
1 ~ n+1
```

---

### 结论 2

�?`x` 的正确位置是�?

```text
x - 1
```

---

### 结论 3

交换条件必须同时满足�?

* 当前值合�?
* 当前值不在正确位�?
* 目标位置不是相同�?

---

### 结论 4

第一个满足：

```text
nums[i] != i + 1
```

的位置，答案就是�?

```text
i + 1
```

---

