
# LeetCode 73. 矩阵置零（C 语言）学习笔�?

## 1. 题目描述

给定一�?`m x n` 的矩�?`matrix`，如果一个元素为 `0`，则将其所在行和所在列的所有元素都设为 `0`�?

要求�?

- 尽量 **原地** 修改矩阵
- 进阶要求：尝试使�?**常量空间** 完成

---

## 2. 题意理解

### �?1

输入�?

```text
[
  [1,1,1],
  [1,0,1],
  [1,1,1]
]
````

输出�?

```text
[
  [1,0,1],
  [0,0,0],
  [1,0,1]
]
```

因为中间那个 `0` 位于�?

* �?1 �?
* �?1 �?

所以整行整列都要变�?`0`�?

---

### �?2

输入�?

```text
[
  [0,1,2,0],
  [3,4,5,2],
  [1,3,1,5]
]
```

输出�?

```text
[
  [0,0,0,0],
  [0,4,5,0],
  [0,3,1,0]
]
```

---

## 3. 容易犯的错误

这道题最容易犯的错误是：

> 一边遍历矩阵，一边直接把所在行列清零�?

这样会导致“新产生�?0”继续扩散，错误地影响后续判断�?

---

### 错误示例

例如矩阵�?

```text
[
  [1,1,1],
  [1,0,1],
  [1,1,1]
]
```

如果扫描�?`matrix[1][1] == 0` 后，立刻把第 1 行和�?1 列清零，就会变成�?

```text
[
  [1,0,1],
  [0,0,0],
  [1,0,1]
]
```

接着继续扫描时，又会看到新出现的 0，于是错误地把更多行列清零�?

所以必须分成两步：

1. 先记录哪些行、哪些列需要清�?
2. 再统一修改

---

## 4. 你的原始思路：使用行标记数组 + 列标记数�?

这是一种很常见、也很容易想到的方法�?

### 思路

* 用一个数�?`row[]` 记录哪些行要清零
* 用一个数�?`col[]` 记录哪些列要清零
* 第一遍扫描矩阵，遇到 `0` 就标记对应行和列
* 第二遍根据标记清�?

---

## 5. 你原代码中的一个问�?

你之前代码中有这段：

```c
if (matrix[i][j] == 0) {
    row[i] = 1;
    col[j] = 1;
    break;
}
```

这里�?`break` 有问题�?

### 为什么错�?

因为它会导致�?

* 某一行一旦遇到第一�?`0`
* 就提前结束这一行的扫描
* 这一行后面如果还有别�?`0`，对应的列就不会被标�?

---

### 反例

例如这一行：

```text
[1, 0, 1, 0]
```

扫描到第二个元素 `0` 时就 `break` 了，后面的第 4 列那�?`0` 不会被记录�?

于是列标记不完整，结果就错了�?

---

## 6. O(m+n) 空间解法

---

### 6.1 思路

定义�?

* `row[i] = 1` 表示�?`i` 行需要清�?
* `col[j] = 1` 表示�?`j` 列需要清�?

步骤�?

1. 第一遍遍历矩�?
   遇到 `matrix[i][j] == 0` 时，设置�?

   ```c
   row[i] = 1;
   col[j] = 1;
   ```

2. 第二遍遍历矩�?
   如果�?

   ```c
   row[i] == 1 || col[j] == 1
   ```

   就把 `matrix[i][j] = 0`

---

### 6.2 C 语言代码

```c
#include <stdlib.h>

void setZeroes(int** matrix, int matrixSize, int* matrixColSize) {
    int m = matrixSize;
    int n = matrixColSize[0];

    int* row = (int*)calloc(m, sizeof(int));
    int* col = (int*)calloc(n, sizeof(int));

    // 第一遍：记录哪些行、哪些列要清�?
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            if (matrix[i][j] == 0) {
                row[i] = 1;
                col[j] = 1;
            }
        }
    }

    // 第二遍：根据标记清零
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            if (row[i] == 1 || col[j] == 1) {
                matrix[i][j] = 0;
            }
        }
    }

    free(row);
    free(col);
}
```

---

### 6.3 复杂度分�?

* 时间复杂度：`O(m*n)`
* 空间复杂度：`O(m+n)`

---

## 7. 进阶：常量空�?O(1) 解法

这才是这道题的经典写法�?

---

### 7.1 核心思想

既然�?

* `row[]` 用来标记�?
* `col[]` 用来标记�?

那么能不能不另外开数组�?

答案是：

> 直接用矩阵的第一行和第一列来充当标记数组�?

也就是：

* �?`matrix[i][0]` 标记�?`i` 行是否需要清�?
* �?`matrix[0][j]` 标记�?`j` 列是否需要清�?

---

### 7.2 但这里有个难�?

第一行和第一列本身也可能原来就有 `0`�?

所以还需要两个额外变量记录：

* `firstRowZero`：第一行原本是否有 `0`
* `firstColZero`：第一列原本是否有 `0`

这两个变量的空间是固定的，所以仍然是 **O(1)**�?

---

## 8. 常量空间解法步骤

### 第一步：检查第一行是否有 0

如果第一行原本有 `0`，最后整行都要清零�?

---

### 第二步：检查第一列是否有 0

如果第一列原本有 `0`，最后整列都要清零�?

---

### 第三步：用第一行和第一列做标记

�?`matrix[1][1]` 开始扫描整个矩阵：

如果 `matrix[i][j] == 0`，则�?

```c
matrix[i][0] = 0;
matrix[0][j] = 0;
```

---

### 第四步：根据标记，把中间区域清零

对所�?`i >= 1` �?`j >= 1` 的位置：

如果�?

```c
matrix[i][0] == 0 || matrix[0][j] == 0
```

则：

```c
matrix[i][j] = 0;
```

---

### 第五步：最后处理第一行和第一�?

因为第一行、第一列既是数据区，又是标记区，所以必须最后单独处理�?

---

## 9. 常量空间解法代码（C 语言�?

```c
void setZeroes(int** matrix, int matrixSize, int* matrixColSize) {
    int m = matrixSize;
    int n = matrixColSize[0];

    int firstRowZero = 0;
    int firstColZero = 0;

    // 1. 检查第一行是否原本有 0
    for (int j = 0; j < n; j++) {
        if (matrix[0][j] == 0) {
            firstRowZero = 1;
            break;
        }
    }

    // 2. 检查第一列是否原本有 0
    for (int i = 0; i < m; i++) {
        if (matrix[i][0] == 0) {
            firstColZero = 1;
            break;
        }
    }

    // 3. 用第一行和第一列作为标�?
    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            if (matrix[i][j] == 0) {
                matrix[i][0] = 0;
                matrix[0][j] = 0;
            }
        }
    }

    // 4. 根据标记，把内部元素�?0
    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            if (matrix[i][0] == 0 || matrix[0][j] == 0) {
                matrix[i][j] = 0;
            }
        }
    }

    // 5. 处理第一�?
    if (firstRowZero) {
        for (int j = 0; j < n; j++) {
            matrix[0][j] = 0;
        }
    }

    // 6. 处理第一�?
    if (firstColZero) {
        for (int i = 0; i < m; i++) {
            matrix[i][0] = 0;
        }
    }
}
```

---

## 10. 常量空间解法详细举例

以矩阵：

```text
[
  [1,1,1],
  [1,0,1],
  [1,1,1]
]
```

为例�?

---

### 10.1 先检查第一行、第一�?

第一行：

```text
[1,1,1]
```

没有 `0`，所以：

```c
firstRowZero = 0
```

第一列：

```text
[1,1,1]
```

也没�?`0`，所以：

```c
firstColZero = 0
```

---

### 10.2 扫描内部元素并打标记

扫描�?`matrix[1][1] == 0`�?

于是�?

```c
matrix[1][0] = 0;
matrix[0][1] = 0;
```

矩阵逻辑上变成：

```text
[
  [1,0,1],
  [0,0,1],
  [1,1,1]
]
```

注意�?

* `matrix[1][0] = 0` 表示�?1 行要清零
* `matrix[0][1] = 0` 表示�?1 列要清零

---

### 10.3 根据标记处理内部区域

检�?`matrix[1][1]`�?

* �?1 行标记为 0
* �?1 列标记也�?0

所以保留为 0�?

检�?`matrix[1][2]`�?

* �?1 行标记为 0

所以改�?0�?

检�?`matrix[2][1]`�?

* �?1 列标记为 0

所以改�?0�?

最终变成：

```text
[
  [1,0,1],
  [0,0,0],
  [1,0,1]
]
```

---

### 10.4 最后处理第一行和第一�?

由于�?

```c
firstRowZero = 0
firstColZero = 0
```

所以第一行和第一列不用整体清零�?

结果正确�?

---

## 11. 为什么这种方法是 O(1) 空间

因为没有额外申请与矩阵规模相关的数组�?

只用了：

```c
int firstRowZero;
int firstColZero;
```

无论矩阵多大，这两个变量都只占固定空间，所以额外空间复杂度是：

```text
O(1)
```

---

## 12. 两种方法对比

| 方法        | 思路                    |    时间复杂�?|    空间复杂�?| 难度 |
| --------- | --------------------- | -------: | -------: | -: |
| 行列标记数组    | 额外开 `row[]` �?`col[]` | `O(m*n)` | `O(m+n)` | 简�?|
| 第一行第一列作标记 | 借用原矩阵存标记              | `O(m*n)` |   `O(1)` | 较难 |

---

## 13. 易错点总结

### 13.1 不能一边遍历一边直接清�?

否则会让新产生的 0 影响后续判断�?

---

### 13.2 使用标记数组时不能提�?`break`

因为一行中可能有多�?`0`，每�?`0` 对应的列都必须被标记�?

---

### 13.3 常量空间法必须先保存第一行和第一列的状�?

因为后面它们要被当作标记位使用�?

---

### 13.4 常量空间法中，内部区域要�?`1` 开始遍�?

即：

```c
for (int i = 1; i < m; i++)
for (int j = 1; j < n; j++)
```

不能�?`0` 开始，否则会破坏第一行第一列的标记意义�?

---

### 13.5 第一行和第一列要最后处�?

因为它们既是原始数据，又承担了“标记数组”的作用�?

---

## 14. 面试时可以怎么�?

这道题有两个层次�?

### 第一层：普通做�?

先用两个数组分别记录哪些行、哪些列需要清零，然后再统一置零�?

* 时间复杂�?`O(m*n)`
* 空间复杂�?`O(m+n)`

---

### 第二层：进阶做法

为了做到常量空间，可以把矩阵的第一行和第一列当作标记数组使用：

* `matrix[i][0]` 标记�?`i` �?
* `matrix[0][j]` 标记�?`j` �?

再额外用两个变量记录第一行和第一列是否原本含 0�?

这样�?

* 时间复杂度仍然是 `O(m*n)`
* 空间复杂度降�?`O(1)`

---

## 15. 推荐记忆模板

这题可以记成一句话�?

> 第一行标列，第一列标行；
> 先存首行首列状态，中间打标记，最后统一清零�?

---

## 16. 最终结�?

这道题的本质是：

> 如何在“不能立即修改”的前提下，记录未来要被清零的信息�?

### 普通解�?

* �?`row[]` �?`col[]` 记录
* 容易理解
* 空间 `O(m+n)`

### 最优解�?

* 用第一行和第一列记�?
* 再用两个变量补充保存首行首列状�?
* 实现常量空间 `O(1)`

---

## 17. 适合背诵的最终代�?

```c
void setZeroes(int** matrix, int matrixSize, int* matrixColSize) {
    int m = matrixSize;
    int n = matrixColSize[0];

    int firstRowZero = 0;
    int firstColZero = 0;

    for (int j = 0; j < n; j++) {
        if (matrix[0][j] == 0) {
            firstRowZero = 1;
            break;
        }
    }

    for (int i = 0; i < m; i++) {
        if (matrix[i][0] == 0) {
            firstColZero = 1;
            break;
        }
    }

    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            if (matrix[i][j] == 0) {
                matrix[i][0] = 0;
                matrix[0][j] = 0;
            }
        }
    }

    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            if (matrix[i][0] == 0 || matrix[0][j] == 0) {
                matrix[i][j] = 0;
            }
        }
    }

    if (firstRowZero) {
        for (int j = 0; j < n; j++) {
            matrix[0][j] = 0;
        }
    }

    if (firstColZero) {
        for (int i = 0; i < m; i++) {
            matrix[i][0] = 0;
        }
    }
}
```

---

## 18. 一句话总结

**这题最经典的地方，不是“置零”，而是“借第一行和第一列存标记”，从而把空间复杂度从 `O(m+n)` 优化�?`O(1)`�?*
