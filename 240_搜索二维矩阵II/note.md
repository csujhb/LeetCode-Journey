

---

# LeetCode 240. 搜索二维矩阵 II 学习笔记

## 1. 题目描述

给定一�?`m x n` 的整数矩�?`matrix`，以及一个目标�?`target`�?

矩阵具有以下性质�?

* 每一行从左到右升序排�?
* 每一列从上到下升序排�?

要求判断 `target` 是否存在于矩阵中�?

存在返回 `true`，否则返�?`false`�?

---

## 2. 示例

### 示例 1

```text id="tchdyv"
输入�?
matrix = [
  [1, 4, 7,11,15],
  [2, 5, 8,12,19],
  [3, 6, 9,16,22],
  [10,13,14,17,24],
  [18,21,23,26,30]
],
target = 5

输出：true
```

---

### 示例 2

```text id="v4s1ae"
输入�?
matrix = [
  [1, 4, 7,11,15],
  [2, 5, 8,12,19],
  [3, 6, 9,16,22],
  [10,13,14,17,24],
  [18,21,23,26,30]
],
target = 20

输出：false
```

---

## 3. 题目本质

这题和普通二维数组查找不一样，因为它同时具有两个单调性：

* 行有�?
* 列有�?

所以矩阵不是完全无序的，也不是“整体展开后完全有序”的那种矩阵�?

这意味着�?

> 不能直接把整个矩阵当成一个一维有序数组做一次二�?

但又可以利用“行列单调”的性质去排除大量元素�?

---

## 4. 错误直觉：为什么不能直接整体二�?

有些矩阵题可以把二维矩阵看成一个整体升序数组，比如�?

```text id="6u8w68"
[
  [1, 3, 5],
  [7, 9, 11]
]
```

因为上一行最后一个元素小于下一行第一个元素�?

但本题不是这样�?

例如�?

```text id="9my6o1"
[
  [1, 4, 7],
  [2, 5, 8],
  [3, 6, 9]
]
```

展开后是�?

```text id="0lv3am"
1, 4, 7, 2, 5, 8, 3, 6, 9
```

这并不是整体升序�?

所以：

> 本题不能直接用“一次二分”解决整个矩阵�?

---

## 5. 最优思路：从右上角开始搜�?

这是本题最经典、最推荐的解法�?

---

## 5.1 为什么选右上角

设当前位置是�?

```text id="41t87a"
matrix[row][col]
```

如果�?**右上�?* 开始，那么这个位置有一个非常好的性质�?

* 它左边的数都更小
* 它下面的数都更大

所以比�?`target` 和当前值后，可以明确知道下一步该往哪走�?

---

## 5.2 具体规则

假设当前在右上角元素 `matrix[row][col]`

### 情况 1：`target == matrix[row][col]`

说明找到了，返回 `true`

---

### 情况 2：`target < matrix[row][col]`

因为当前列从上到下递增，所以这一列当前元素下面的所有元素都更大，不可能是答案�?

因此�?

> 当前列可以排除，应该 **左移**

即：

```c id="2s2j1v"
col--;
```

---

### 情况 3：`target > matrix[row][col]`

因为当前行从左到右递增，所以这一行当前元素左边的所有元素都更小，不可能是答案�?

因此�?

> 当前行可以排除，应该 **下移**

即：

```c id="rhbux1"
row++;
```

---

## 6. 算法流程

1. 从右上角出发�?

   * `row = 0`
   * `col = n - 1`

2. �?`row` 没出界且 `col` 没出界时�?

   * 若当前值等于目标值，返回 `true`
   * 若目标值更小，左移
   * 若目标值更大，下移

3. 如果走出边界还没找到，返�?`false`

---

## 7. C语言代码实现

你写的代码其实已经是标准答案了�?

```c id="2l20zr"
#include <stdbool.h>

bool searchMatrix(int** matrix, int matrixSize, int* matrixColSize, int target) {
    if (matrixSize == 0 || matrixColSize[0] == 0) {
        return false;
    }

    int row = 0;
    int col = matrixColSize[0] - 1;

    while (row < matrixSize && col >= 0) {
        if (target < matrix[row][col]) {
            col--;
        }
        else if (target > matrix[row][col]) {
            row++;
        }
        else {
            return true;
        }
    }

    return false;
}
```

---

## 8. 结合示例理解过程

以：

```text id="ewyh99"
matrix = [
  [1, 4, 7,11,15],
  [2, 5, 8,12,19],
  [3, 6, 9,16,22],
  [10,13,14,17,24],
  [18,21,23,26,30]
]
target = 5
```

为例�?

---

### 第一步：从右上角开�?

右上角是�?

```text id="cyvekc"
15
```

比较�?

```text id="fi27h3"
5 < 15
```

说明 `15` 这一列当前及其下方都太大，所以左移�?

---

### 第二步：来到 `11`

比较�?

```text id="3r3qjd"
5 < 11
```

继续左移�?

---

### 第三步：来到 `7`

比较�?

```text id="ijvqk3"
5 < 7
```

继续左移�?

---

### 第四步：来到 `4`

比较�?

```text id="x40xvn"
5 > 4
```

说明这一行左边都更小，不可能是答案，所以向下移动�?

---

### 第五步：来到 `5`

比较�?

```text id="rbyygj"
5 == 5
```

找到目标，返�?`true`

---

## 9. 为什么这种方法是对的

每一步都能安全排除一整行或一整列�?

* 向左走：排除当前�?
* 向下走：排除当前�?

而不会漏掉目标值�?

因为右上角这个位置恰好具有：

* 左边更小
* 下边更大

这种“夹在中间”的单调特性�?

这使得每一步决策都是明确的�?

---

## 10. 复杂度分�?

设：

* `m = matrixSize`，行�?
* `n = matrixColSize[0]`，列�?

---

### 时间复杂�?

每一步要么：

* `row++`
* 要么 `col--`

所以总共最多移动：

```text id="lfok34"
m + n
```

步�?

因此时间复杂度为�?

```text id="38m3v1"
O(m + n)
```

---

### 空间复杂�?

只用了几个变量：

* `row`
* `col`

所以空间复杂度为：

```text id="vhwo4t"
O(1)
```

---

## 11. 这题还有没有其他做法

有，但通常都不如当前方法实用�?

---

### 11.1 逐行二分查找

对每一行做二分�?

#### 复杂�?

* 时间复杂度：`O(m log n)`
* 空间复杂度：`O(1)`

#### 特点

当矩阵很“矮而宽”时，可能比较合适�?
但一般情况下不如右上角搜索法常用�?

---

### 11.2 逐列二分查找

对每一列做二分�?

#### 复杂�?

* 时间复杂度：`O(n log m)`
* 空间复杂度：`O(1)`

#### 特点

当矩阵很“高而窄”时，可能比较合适�?

---

### 11.3 分治

有些做法会从中间行或中间列切分矩阵递归搜索�?

#### 特点

* 写法更复�?
* 面试中不如右上角搜索直观
* 实战里不如本题标准解法简�?

---

## 12. 到底哪个更好

### 对这题最推荐的写�?

就是你现在写的：

> **从右上角开始，按大小比较，决定左移或下�?*

因为它具有以下优点：

* 思路清晰
* 代码�?
* 易于证明正确�?
* 时间复杂度优秀
* 空间复杂度最�?

---

### 更严格一点的理论比较

有时可以比较�?

[
O(\min(m+n,\ m\log n,\ n\log m))
]

也就是说�?

* 正常情况下，`O(m+n)` 很优秀
* 如果矩阵非常扁或非常瘦，逐行/逐列二分可能更快

�?LeetCode 和面试中，通常不会要求你做这种细分优化�?

---

## 13. 易错点总结

### 13.1 不能把这题和 LeetCode 74 混淆

LeetCode 74 的矩阵满足：

* 每行有序
* 且每行第一个元素大于上一行最后一个元�?

那种题可以整体二分�?

本题不行�?

---

### 13.2 右上角和左下角都可以，别选错方向

如果从右上角开始：

* 小了左移
* 大了下移

如果从左下角开始：

* 小了上移？不�?
* 大了右移？也要重新分�?

所以最常用的是�?

> **右上角开�?*
> 或�?
> **左下角开�?*

但方向规则必须配套写对�?

---

### 13.3 记得处理空矩�?

要加上：

```c id="7f5rr9"
if (matrixSize == 0 || matrixColSize[0] == 0) {
    return false;
}
```

避免越界访问�?

---

### 13.4 不要写成暴力搜索

逐个元素遍历当然能做，但复杂度是�?

```text id="3m2ive"
O(mn)
```

没有利用矩阵的单调性�?

---

## 14. 模板总结

这题可以记成下面这个模板�?

```c id="ck6h2m"
从右上角开始：
- 如果 target 比当前值小，左�?
- 如果 target 比当前值大，下�?
- 如果相等，返�?true
- 越界后返�?false
```

代码模板�?

```c id="gkgwl1"
bool searchMatrix(int** matrix, int matrixSize, int* matrixColSize, int target) {
    if (matrixSize == 0 || matrixColSize[0] == 0) {
        return false;
    }

    int row = 0;
    int col = matrixColSize[0] - 1;

    while (row < matrixSize && col >= 0) {
        if (matrix[row][col] == target) {
            return true;
        } else if (target < matrix[row][col]) {
            col--;
        } else {
            row++;
        }
    }

    return false;
}
```

---

## 15. 面试答题模板

如果面试官问思路，可以这样答�?

> 这个矩阵每行递增、每列递增，因此适合从右上角开始搜�?�?
> 右上角元素左边都更小，下面都更大�?
> 如果目标值小于当前值，就说明当前列不可能包含答案，应向左移动；
> 如果目标值大于当前值，就说明当前行不可能包含答案，应向下移动；
> 如果相等则直接返�?true�?
> 这样每一步都能排除一整行或一整列，最多移�?`m+n` 步，因此时间复杂度是 `O(m+n)`，空间复杂度�?`O(1)`�?

---

## 16. 一句话总结

> **搜索二维矩阵 II 的核心是：从右上角开始，每次排除一整行或一整列�?*

---

## 17. 适合记住的结�?

### 结论 1

本题不能整体二分，因为矩阵整体不是严格升序展开�?

### 结论 2

最经典解法是从右上角开始搜�?�?

### 结论 3

每一步都能排除一整行或一整列�?

### 结论 4

时间复杂度是�?

```text id="4wsito"
O(m + n)
```

### 结论 5

空间复杂度是�?

```text id="tsghei"
O(1)
```

---


