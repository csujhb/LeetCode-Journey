// 做不出来
#include <stdlib.h>
int maxProduct(int *nums, int numsSize)
{
    int neg_abs_max = 1;
    int min_plus = 1;
    int res = 1;
    for (int i = 0; i < numsSize; i++)
    {
        int cur = nums[i];
    }
}