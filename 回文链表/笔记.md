??

---

# LeetCode 234. 回文链�?? 学习笔记

## 1. 题目描述

给你一个单链表的头结�?? `head`，判断该链表是否为回文链表�???

如果是，返�?? `true`；否则返???`false`???

---

## 2. 示例

### 示例 1

```text id="2qijgp"
输入：head = [1,2,2,1]
输出：true
```

---

### 示例 2

```text id="wm3qea"
输入：head = [1,2]
输出：false
```

---

## 3. 题目本质

这题本质上是在问???

> 如何判断一个单链表从前往后读和从后往前读是否相同???

对于数组来说，这件事很简单：

* 直接双指针从两端往中间比较

但链表没有随机访问能力，不能像数组那样直接拿到“倒数第几个元素”�???

所以难点在于�??

> **怎么在链表上模拟“两端比较”�??**

---

## 4. 常见做法概�??

---

## 4.1 转成数组

### 思�??

* 遍历链表，把所有值存进数???
* 然后用双指针判断数组是否回�??

### 复杂�?

* 时间复杂度：`O(n)`
* 空间复杂度：`O(n)`

### 优�??

* 最简???
* 最容易�?

### 缺�??

* 不满足题目进阶要???`O(1)` 额外空间

---

## 4.2 �q????

### 思�??

* 遍历链表，把值压入栈
* 再遍历一次链表，同时和栈顶元素比???

或者�??

* 只把前半段压栈，再和后半段比�?

### 复杂�?

* 时间复杂度：`O(n)`
* 空间复杂度：`O(n)` ???`O(n/2)`

### 优�??

* 仍然比较容易理�??

### 缺�??

* 仍然用了额外空间

---

## 4.3 递�??

### 思�??

利用递归先走到链表尾部，再在回溯阶段与头部同步比较�???

### 复杂�?

* 时间复杂度：`O(n)`
* 空间复杂度：`O(n)`（递归栈）

### 优�??

* 写法巧�??

### 缺�??

* 栈空间不???`O(1)`
* 链表很长时可能栈深过�?

---

## 4.4 快慢指针 + 反转后半段链�?

这就是这题最经典、最优的做法�?

### 复杂�?

* 时间复杂度：`O(n)`
* 空间复杂度：`O(1)`

### 特�??

* 满足进阶要求
* 面试中最推�??
* 是本题标准解???

---

## 5. 最优解核心思路

这题最优解可以分成三步???

1. �q??��慢指针找到链表中�?
2. 反转后半段链�?
3. 从两端向中间比较

---

## 6. 第一步：快慢指针找中�?

定义两个指针�?

* `slow` 每次走一???
* `fast` 每次走两???

???`fast` 走到链表末尾时，`slow` 就到达中点附近�?

---

### 偶数长度情�??

例如�?

```text id="zn6c79"
1 -> 2 -> 2 -> 1
```

最终�??

* `slow` 指向后半段起�?
* `fast == NULL`

---

### 奇数长度情�??

例如�?

```text id="cv2v3n"
1 -> 2 -> 3 -> 2 -> 1
```

最终�??

* `slow` 指向中间节�?? `3`
* `fast != NULL`

此时中间节点不影响回文性，所以一般会�?

```c id="0m207t"
if (fast != NULL) {
    slow = slow->next;
}
```

跳过中间节点，再处理后半段�???

---

## 7. 第二步：反转后半段链�?

反转后半段后，就可以把：

* 前半段从左往�?
* 后半段从右往�?

变成两个都能“顺�?? next 指针往前走”的链表�?

例如�?

原链表：

```text id="52k1dc"
1 -> 2 -> 2 -> 1
```

后半�?`2 -> 1` 反转后：

```text id="t8pt98"
1 -> 2
```

现在就可以拿�?

* 前半�?`1 -> 2`
* 反转后的后半�?`1 -> 2`

逐个比较???

---

## 8. 第三步：逐节点比???

定义两个指针�?

* `first` 指向头结???
* `second` 指向反转后的后半段头结点

然后同时往后走�?

* 如果某一对值不同，返�?? `false`
* 如果全部相同，返???`true`

---

## 9. 标�?? C 代码实�??

```c id="f8fpb3"
#include <stdbool.h>

/**
 * Definition for singly-linked list.
 */
struct ListNode {
    int val;
    struct ListNode *next;
};

struct ListNode* reverseList(struct ListNode* head) {
    struct ListNode* prev = NULL;
    struct ListNode* cur = head;

    while (cur != NULL) {
        struct ListNode* next = cur->next;
        cur->next = prev;
        prev = cur;
        cur = next;
    }

    return prev;
}

bool isPalindrome(struct ListNode* head) {
    if (head == NULL || head->next == NULL) {
        return true;
    }

    struct ListNode* slow = head;
    struct ListNode* fast = head;

    // 1. 找中�?
    while (fast != NULL && fast->next != NULL) {
        slow = slow->next;
        fast = fast->next->next;
    }

    // 2. 奇数长度时跳过中间节�?
    if (fast != NULL) {
        slow = slow->next;
    }

    // 3. 反转后半???
    struct ListNode* second = reverseList(slow);
    struct ListNode* secondCopy = second;   // 备份，便于恢�?
    struct ListNode* first = head;

    // 4. 比较两半
    bool ok = true;
    while (second != NULL) {
        if (first->val != second->val) {
            ok = false;
            break;
        }
        first = first->next;
        second = second->next;
    }

    // 5. 恢复链表（可选，但推荐�??
    reverseList(secondCopy);

    return ok;
}
```

---

## 10. 你当前代码的思路说�??

你写的代码是�?

```c id="94srrr"
bool isPalindrome(struct ListNode* head) {
    struct ListNode* slow = head;
    struct ListNode* fast = head;
    while (fast != NULL && fast->next != NULL) {
        slow = slow->next;
        fast = fast->next->next;
    }
    reverseList(&slow);
    fast = head;
    while (fast != NULL && slow != NULL) {
        if (fast->val != slow->val) return false;
        slow = slow->next;
        fast = fast->next;
    }
    return true;
}
```

这也是正确思路，属于�??

* 找中�?
* 直接�?`slow` 开始反�?
* 再比�?

这种写法也是可行的�???

---

## 11. 你的写法和标准写法的区别

---

### 11.1 你的写法特�??

你是这样做的�?

* `slow` 走到中点
* 不区分奇???
* 直接�?`slow` 开始反�?

这样在奇数长度链表里，中间节点也会被反转进去�?

例如�?

```text id="1r4jwd"
1 -> 2 -> 3 -> 2 -> 1
```

???`slow` 指�?? `3` 时，反转???

```text id="1awj7v"
3 -> 2 -> 1   反转后变???  1 -> 2 -> 3
```

再�?? `head` 开始比较，仍然能得到正确答案�???

所以�??

> 你的写法是对的，不一定非要跳过中间节点�???

---

### 11.2 标准写法特�??

标准写法会额外区分奇偶长度�??

* 偶数长度：直接反转后半段
* 奇数长度：先跳过中间节点，再反�??

这样逻辑会更清晰，语义也更直观�?

---

## 12. 你的代码的优点与不�??

---

### 优�??

* 时间复杂度已经最优：`O(n)`
* 空间复杂度已经最优：`O(1)`
* `reverseList` 封装独立，结构不???
* 实现相对简???

---

### 不�??

#### 1）会破坏原链表结???

你比较完之后，没有把链表恢复???

虽�?? LeetCode 一般不强制要求恢复，但从工程习惯和面试习惯来看，通常更推荐：

> 比较完成后，把后半段再反转回�?

---

#### 2）奇偶逻辑不够显式

你的写法虽然是对的，但对初学者来说不如“奇数跳过中间节点”那种写法直观�???

---

## 13. 为什么这题已经没有更优做法�??

你这类做法的复杂度是�?

* 时间：`O(n)`
* 空间：`O(1)`

这是最优的???

原因�?

### 时间下界

你至少要把链表看一遍，所以时间复杂度不可能优于�??

```text id="9j0l2l"
O(n)
```

### 空间下界

你的方法只用了几个指针变量，所以额外空间已经是�?

```text id="6cq1mk"
O(1)
```

无法再更低�???

所以这题没有比它更优的渐近复杂度算法�?

---

## 14. 结合示例理解过�??

### 示例 1

```text id="av17wb"
head = [1,2,2,1]
```

链表�?

```text id="kclmt8"
1 -> 2 -> 2 -> 1
```

---

### 第一步：找中�?

快慢指针走完后：

* `slow` 指向后半段起�?`2`
* `fast == NULL`

---

### 第二步：反转后半???

后半段：

```text id="7b5ljr"
2 -> 1
```

反转后：

```text id="6akbpo"
1 -> 2
```

---

### 第三步：比较两半

前半段：

```text id="qejavj"
1 -> 2
```

反转后的后半段：

```text id="l6d5f8"
1 -> 2
```

逐个比较，全部相等，所以返回�??

```text id="3kszhq"
true
```

---

### 示例 2

```text id="cm4zv4"
head = [1,2]
```

链表�?

```text id="a20v4r"
1 -> 2
```

中点后半段反转仍是�??

```text id="5kiyci"
2
```

比较时：

* `1 != 2`

所以返回：

```text id="wi52gx"
false
```

---

## 15. 复杂度分�?

### 时间复杂???

找中点一次：`O(n)`
反转后半段一次：`O(n)`
比较一次：`O(n)`
恢复一次（可选）：`O(n)`

总复杂度仍然是：

```text id="ho3ikl"
O(n)
```

---

### 空间复杂???

只用了若干个指针变量�?

```text id="z6c4c3"
O(1)
```

---

## 16. 易错点总结

### 16.1 奇数长度链表要不要跳过中间节�?

可以有两种写法：

#### 写法一：跳过中间节???

更标准、更直观�?

#### 写法二：不跳过，直接从中点反�?

只要比较逻辑写对，也可以通过�?

你的代码属于第二种�???

---

### 16.2 注意反转的是“后半段”，不是整个链�??

否则前半部分会丢失原结构，比较逻辑也会错�???

---

### 16.3 比较时只需要比较后半段长度

通常这样写�??

```c id="8mop5v"
while (second != NULL)
```

因为后半段长度一定小于等于前半段???

---

### 16.4 记得考虑空链表和单节点链???

这些情况一定是回文???

---

### 16.5 如果题目或面试关心副作用，记得恢复链�?

恢复不是必须，但通常是加分项�?

---

## 17. 可选的同级别最优写法：一边找中点一边反转前半段

除了“反转后半段”之外，还有一种同样最优的写法�?

> 快慢指针向前走时，顺便把前半段原地反�?

到中点之后，再比较�??

* 反转后的前半�?
* 原始后半???

这种写法复杂度同样是???

* 时间：`O(n)`
* 空间：`O(1)`

但实现更绕一些，一般不如“反转后半段”常用�?

所以�??

> 有“同样最优但写法不同”的方法，但没有“更优”的方法???

---

## 18. 面试答题模�??

如果面试官问你思路，可以这样回答：

> 这题要求判断单链表是否为回文，最优做法是使用快慢指针找到中点，然后原地反转后半段链表，再从头结点和后半段头结点同时向后比较�???
> 如果链表长度是奇数，可以先跳过中间节点，因为中间节点不影响回文性�?
> 比较完成后，如果需要保持原链表不变，还可以再把后半段恢复回来�???
> 这样做的时间复杂度�?? `O(n)`，额外空间复杂度???`O(1)`，已经达到了最优�?

---

## 19. 一句话总结

> **回文链表最优�?? = 快慢指针找中�?+ 原地反转后半链�?? + 两端比较**

---

## 20. 适合记住的结�?

### 结�?? 1

数组法最简单，但空间�?? `O(n)`???

### 结�?? 2

本题最优解是：

* 时�?? `O(n)`
* 空�?? `O(1)`

### 结�?? 3

最经典方法是：

* 找中�?
* 反转后半???
* 逐个比较

### 结�?? 4

比较完成后，最好把链表恢复回来�?

### 结�?? 5

这题没有???`O(n)` 时间、`O(1)` 空间更优的做法�?

---

